MainLayout:
    Button:
        id: pb_move
        font_size: '18sp'
        size: (285, 48)
        size_hint: (None, None)
        pos_hint: {'center_x': 0.5, 'center_y': 0.5}
        text: "Let's move it, move it, move it!"
        on_press: root.move_it()
